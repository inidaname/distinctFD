function Header(): JSX.Element {
  return (
    <header className="w-full bg-gradient-to-bl from-red-400 via-red-500 to-red-400 h-16 flex flex-col justify-between items-center">
      see
    </header>
  );
}

export default Header;
